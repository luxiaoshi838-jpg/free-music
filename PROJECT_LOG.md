# PROJECT LOG

> 说明：该文件记录公开仓库的重要版本变更。历史内容在此前提交中保存；本次文件从 v152 起继续记录。

## v152 — 连续快速切歌稳定性与系统退出报告

- 版本：`2026.08.05.v152-rapid-next-stability`
- versionCode：`2026080152`
- 保留 v151 的搜索 Range 分段缓存实现。
- 手动连续点击上一首/下一首在 220 ms 窗口内累积，最终只执行一次目标跳转。
- MediaPlayer 创建、`setDataSource` 和异步准备入口改为单线程串行执行。
- 新播放请求取消旧媒体打开任务及旧搜索后台缓存任务。
- 释放 MediaPlayer 前移除 Prepared、Completion、Error 回调，再执行 stop/reset/release。
- 搜索后台缓存加入线程中断检查，旧切歌任务可尽快停止。
- Android 11 及以上在下次启动时读取 `ApplicationExitInfo`，补充 Native crash、ANR、低内存、信号终止等系统退出报告。
- 播放切换前写入歌曲与请求序号线索，播放成功后清除；异常退出报告可关联最后一次切歌。
- 已合并原有和新增的 `onDestroy()` 清理，保留进度保存、接收器注销、服务停止和看门狗清理，并增加新执行器关闭。
- v152 专项检查、v151 Range 回归检查、四品牌编译、包名和版本号检查全部通过。
- GitHub Actions：run `31020681642`，artifact `8936494274`。

手机端仍需进行连续快速切歌压力测试；自动构建不能证明所有设备的原生播放器崩溃均已消失。
