# 四品牌音乐软件源码——本地部署说明

## 包含的四个品牌

| 品牌 | Android 包名 | Gradle 任务 |
|---|---|---|
| 大宝贝儿老婆 | `com.jianglab.babywife` | `assembleBabywifeclassicDebug` |
| 李大财主 | `com.jianglab.babywife.lidacaizhu` | `assembleLidacaizhuDebug` |
| 姜Lab | `com.jianglab.babywife.jianglab` | `assembleJianglabDebug` |
| 牛逼 | `com.jianglab.babywife.niubi` | `assembleNiubiDebug` |

四个版本功能相同，应用名称、包名、图标和默认背景不同。

## 本地构建环境

推荐直接使用 Android Studio 打开本目录。工程要求：

- JDK 17
- Android SDK Platform 35
- Android SDK Build Tools 35.0.0
- Gradle 8.7
- Android Gradle Plugin 8.5.2

音乐桥已经以 `app/libs/musicbridge.aar` 放入工程，普通本地构建不需要安装 Go 或 gomobile。

## Gradle Wrapper（推荐）

压缩包没有捆绑第三方 Gradle 二进制。首次在已安装 Gradle 8.7 的电脑上执行：

```powershell
.\初始化GradleWrapper.ps1
```

或：

```bash
./init_gradle_wrapper.sh
```

生成 `gradlew.bat` / `gradlew` 后，后续构建脚本会优先使用 Wrapper，便于固定 Gradle 8.7。

## Windows 一键构建

在 PowerShell 中进入源码目录后运行：

```powershell
.\构建四品牌APK.ps1
```

## Linux / WSL 构建

```bash
./build_four_apks.sh
```

两个脚本都包含“单步骤连续五分钟没有新增日志就停止”的卡住检查。成功后，四个 APK 位于 `本地构建输出/`。

## 签名说明

本压缩包不包含私有正式签名文件、密码或 GitHub Actions 签名缓存。默认 Debug 构建使用本机生成的 Android 调试证书：

- 可以安装和测试；
- 四个包名互不冲突，可以同时安装；
- 不能直接覆盖安装使用原私有正式证书签名的旧 APK。

需要覆盖旧版本时，应只在你自己的电脑上配置原正式签名，不要把签名文件和密码提交到公开仓库。

## 源码来源

工程主体来自四品牌开发分支最终成功构建状态，主界面、歌词匹配器和歌词选择器采用成功构建时保存的最终生成源码；四品牌图标和背景资源从同一次验证通过的 APK 产物中逐项提取；音乐桥采用对应成功构建产物中的 AAR。
