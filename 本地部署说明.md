# 四品牌音乐软件源码——本地部署说明

## 四个品牌

| 品牌 | Android 包名 | Gradle 任务 |
|---|---|---|
| 大宝贝儿老婆 | `com.jianglab.babywife` | `assembleBabywifeclassicDebug` |
| 李大财主 | `com.jianglab.babywife.lidacaizhu` | `assembleLidacaizhuDebug` |
| 姜Lab | `com.jianglab.babywife.jianglab` | `assembleJianglabDebug` |
| 牛逼 | `com.jianglab.babywife.niubi` | `assembleNiubiDebug` |

四个版本共享功能代码，名称、包名、图标和默认背景不同。姜Lab 单独启用首次启动验证。

## 环境

- JDK 17
- Android SDK Platform 35
- Android SDK Build Tools 37.0.0
- Gradle 8.7
- Android Gradle Plugin 8.5.2

音乐桥位于 `app/libs/musicbridge.aar`，普通构建不需要 Go 或 gomobile。

## 配置姜Lab 私有验证

真实口令和哈希不能提交到公开仓库。运行：

```powershell
python scripts/generate_jianglab_passphrase_hash.py
```

复制 `private.properties.example` 为 `private.properties`，填入脚本生成的 64 位 SHA-256：

```properties
JIANG_LAB_PASSPHRASE_SHA256=<64位SHA-256>
```

验证成功后，姜Lab 只在本机保存已验证状态，之后不再要求输入。卸载应用或清除应用数据后会重新验证。其他三个品牌不受影响。

## 构建

Windows：

```powershell
.\构建四品牌APK.ps1
```

Linux / WSL：

```bash
./build_four_apks.sh
```

两个脚本均要求姜Lab 私有哈希有效，并执行单步骤五分钟无新增日志检查。成功后四个 APK 位于 `本地构建输出/`。

## 缓存目录

- 未选择外部位置时使用应用默认内部目录。
- 设置中点击“歌单缓存位置”可选择缓存总文件夹；长按可恢复默认。
- 总文件夹内自动创建：
  - 每个歌单对应的同名子文件夹；
  - `缓存` 子文件夹，用于搜索和替换产生的临时歌曲及歌词。
- 搜索歌曲加入歌单后，已有缓存会迁移到歌单目录。
- 顶部扫把按钮只清理 `缓存` 子文件夹；被清理的替换版本下次播放会重新缓存。

## 签名

公开仓库不包含正式签名文件和密码。默认 Debug APK 可以并行安装测试，但不能覆盖旧正式签名版本。
