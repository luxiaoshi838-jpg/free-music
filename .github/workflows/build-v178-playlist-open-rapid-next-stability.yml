name: Build v178 Playlist Open Rapid Next Stability

on:
  push:
    branches: [agent/v178-playlist-open-rapid-next-stability]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/v178-playlist-open-rapid-next-stability
          fetch-depth: 0

      - name: Apply v178 patch
        run: |
          set -euo pipefail
          if grep -q 'versionCode 2026080878' app/build.gradle \
             && grep -q 'notifyPlaylistAdapterStateChanged' app/src/main/java/com/jianglab/babywife/MainActivity.java \
             && grep -q 'PLAYER_THREAD = new HandlerThread' app/src/main/java/com/jianglab/babywife/UnifiedMediaPlayer.java; then
            echo 'v178 patch already applied; skip'
          else
            python3 scripts/apply_v178_playlist_open_rapid_next_stability.py
          fi
          if ! git diff --quiet; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add app/build.gradle \
              app/src/main/java/com/jianglab/babywife/MainActivity.java \
              app/src/main/java/com/jianglab/babywife/UnifiedMediaPlayer.java
            git commit -m 'v178: keep playlist open and rapid next off UI hot paths [skip ci]'
            git push origin HEAD:agent/v178-playlist-open-rapid-next-stability
          fi

      - name: Verify source
        run: |
          set -euo pipefail
          grep -q 'versionCode 2026080878' app/build.gradle
          grep -q 'versionName "2026.08.10.v178-playlist-open-rapid-next-stability"' app/build.gradle
          grep -q 'songHasRecordedCacheQuick(song)' app/src/main/java/com/jianglab/babywife/MainActivity.java
          grep -q 'changed |= applyRecoveredCacheState(song, uri);' app/src/main/java/com/jianglab/babywife/MainActivity.java
          grep -q 'notifyPlaylistAdapterStateChanged' app/src/main/java/com/jianglab/babywife/MainActivity.java
          grep -q 'if (!activityResumed || !windowFocused)' app/src/main/java/com/jianglab/babywife/MainActivity.java
          grep -q 'PLAYER_THREAD = new HandlerThread("babywife-media3-player")' app/src/main/java/com/jianglab/babywife/UnifiedMediaPlayer.java
          grep -q 'setLooper(PLAYER_THREAD.getLooper())' app/src/main/java/com/jianglab/babywife/UnifiedMediaPlayer.java
          grep -q 'PLAYER_HANDLER.post(this::releaseInternalPlayer)' app/src/main/java/com/jianglab/babywife/UnifiedMediaPlayer.java
          grep -q 'private void requestSnapshot()' app/src/main/java/com/jianglab/babywife/UnifiedMediaPlayer.java
          grep -q 'playlistCacheProgressText(song, index, targets.size())' app/src/main/java/com/jianglab/babywife/MainActivity.java
          grep -q 'probe.durationMs <= 60_000L' app/src/main/java/com/jianglab/babywife/PlayableAudioResolver.java
          grep -q 'isManualOnlyCacheSong(song)' app/src/main/java/com/jianglab/babywife/MainActivity.java
          grep -q 'next.setAudioAttributes(audioAttributes, true);' app/src/main/java/com/jianglab/babywife/UnifiedMediaPlayer.java

      - name: Prepare Android toolchain
        run: |
          set -euo pipefail
          export JAVA_HOME="${JAVA_HOME_17_X64}"
          export PATH="$JAVA_HOME/bin:$PATH"
          export ANDROID_HOME="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          export ANDROID_SDK_ROOT="$ANDROID_HOME"
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-35' 'build-tools;37.0.0'
          curl -fL --retry 5 --retry-delay 5 https://services.gradle.org/distributions/gradle-8.7-bin.zip -o /tmp/gradle-8.7-bin.zip
          rm -rf /tmp/gradle-8.7
          unzip -q /tmp/gradle-8.7-bin.zip -d /tmp

      - name: Build four unsigned releases
        env:
          JIANG_LAB_PASSPHRASE_SHA256: ${{ secrets.JIANG_LAB_PASSPHRASE_SHA256 }}
        run: |
          set -euo pipefail
          export JAVA_HOME="${JAVA_HOME_17_X64}"
          export ANDROID_HOME="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          export ANDROID_SDK_ROOT="$ANDROID_HOME"
          export PATH="$JAVA_HOME/bin:/tmp/gradle-8.7/bin:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          mkdir -p build
          /tmp/gradle-8.7/bin/gradle --no-daemon assembleRelease 2>&1 | tee build/assemble-v178-release.log

      - name: Align and verify APKs
        run: |
          set -euo pipefail
          export ANDROID_HOME="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          aapt="$ANDROID_HOME/build-tools/37.0.0/aapt"
          zipalign="$ANDROID_HOME/build-tools/37.0.0/zipalign"
          mkdir -p v178-unsigned-aligned signing-tools/lib
          declare -A expected
          expected[babywifeclassic]='com.jianglab.babywife'
          expected[lidacaizhu]='com.jianglab.babywife.lidacaizhu'
          expected[jianglab]='com.jianglab.babywife.jianglab'
          expected[niubi]='com.jianglab.babywife.niubi'
          for flavor in babywifeclassic lidacaizhu jianglab niubi; do
            input="app/build/outputs/apk/${flavor}/release/app-${flavor}-release-unsigned.apk"
            output="v178-unsigned-aligned/${flavor}_v178_unsigned_aligned.apk"
            test -s "$input"
            "$zipalign" -f -p 4 "$input" "$output"
            "$zipalign" -c -p 4 "$output"
            badging=$("$aapt" dump badging "$output")
            package_name=$(printf '%s\n' "$badging" | sed -n "s/^package: name='\([^']*\)'.*/\1/p")
            version_code=$(printf '%s\n' "$badging" | sed -n "s/^package:.*versionCode='\([^']*\)'.*/\1/p")
            test "$package_name" = "${expected[$flavor]}"
            test "$version_code" = '2026080878'
          done
          cp "$ANDROID_HOME/build-tools/37.0.0/apksigner" signing-tools/
          cp "$ANDROID_HOME/build-tools/37.0.0/zipalign" signing-tools/
          cp "$ANDROID_HOME/build-tools/37.0.0/lib/apksigner.jar" signing-tools/lib/
          chmod +x signing-tools/apksigner signing-tools/zipalign
          sha256sum v178-unsigned-aligned/*.apk > v178-unsigned-aligned/SHA256.txt
          git rev-parse HEAD > v178-unsigned-aligned/SOURCE_HEAD.txt

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: v178-unsigned-aligned-and-signing-tools
          path: |
            v178-unsigned-aligned/
            signing-tools/
            build/assemble-v178-release.log
          if-no-files-found: error
          retention-days: 7
